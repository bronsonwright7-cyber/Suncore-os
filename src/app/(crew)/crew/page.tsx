export default function CrewHomePage() {
  return (
    <div className="flex flex-col gap-2">
      <h1 className="text-foreground text-lg font-semibold">Crew</h1>
      <p className="text-muted-foreground text-sm">
        Authentication and foundation only -- the mobile-friendly crew job view is built in a later
        phase.
      </p>
    </div>
  );
}
